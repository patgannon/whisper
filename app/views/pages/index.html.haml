%h1 Managing Web Pages for project #{@project.name}

= link_to 'Make a web page', new_resource_url if can? :manage, @project

%ul#pages.items
  - @pages.each do |page|
    %li.item[page]
      %ul.details
        %li.field.title
          %span.field_name Title
          %span.field_value= page.title
      = link_to 'Show', project_page_path(@project, page)
      = link_to 'Edit', edit_project_page_path(@project, page) if can? :edit, page
      = link_to 'Destroy', project_path_path(@project, page), :confirm => 'Are you sure?', :method => :delete if can? :destroy, page

%br

