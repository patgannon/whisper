%h1 Sign up for a class today!

%p
  You may sign up for a class by paying the entire amount, or else put down a deposit for a space in the class.

= paginate @products


- for product in @products
  .product
    %h2= product.name
    %table
      %tr
        %td= product.price
        %td= link_to 'Purchase', product.buy_now_url('/')
        - if can? :manage, product
          %td= link_to 'Edit', edit_product_path(product)
          %td= link_to 'Destroy', product, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Product', new_product_path if can? :manage, @project
