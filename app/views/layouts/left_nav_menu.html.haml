!!!
%html
  %head
    %title Whisper
    = javascript_include_tag :defaults
    = stylesheet_link_tag "web_main"
    = yield :head
    = csrf_meta_tag
    = javascript_tag "const AUTH_TOKEN = #{form_authenticity_token.inspect};" if protect_against_forgery?
  %body
    #header
      .main_title Whisper...
      .main_caption There's no need to shout.
      #main_menu
        %ul
          - for menu_item in @layout.menus[:main_menu]
            %li= link_to menu_item.title, menu_item.url
    #content_wrapper
      #flash
        - if flash[:notice]
          .flash= flash[:notice]
        - if flash[:error]
          .flash_error= flash[:error]
      #content
        = yield

